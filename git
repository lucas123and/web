git config --global user.name "lucas"
git config --global user.email "lucasandrada141@gmail.com"
git config --list para ver si se coloco
cd c: para crear acceder a c
cd ../ para volver al lugar anterior
mkdir repo para crear carpeta
rmdir para borrar carpeta
cd repo para acceder a repo
git init para iniciar git en esa carpeta
git add archivo.txt para preparar el archivo para subir al repositorio
git status para ver si esta listo
git commit -m "agregando los archivos" para inicializar la subida

clonar repo
mkdir repo-nuevo
cd repo-nuevo
code .
git clone https://github.com/lucas123and/web.git




